import classes from '../style/lodingCard.module.css'


const LoadingCard = () => {

    return (
        <div className={`${classes.card} p-4 shadow-xl cursor-pointer`}>
        </div>
    )
}

export default LoadingCard